{
  "name": "stepboard-tests",
  "private": true,
  "type": "module",
  "description": "Headless browser checks for the panel. The app itself lives in ui/.",
  "scripts": {
    "test": "node tests/parity.mjs && node tests/regressions.mjs && node tests/drag-select.mjs && node tests/branches.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
